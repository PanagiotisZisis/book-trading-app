'use strict';

$(document).ready(function() {

  // burger button
  $(".button-collapse").sideNav();

  // var socket = io();

  /*socket.on('message', function(msg) {
    console.log(msg);

    socket.emit('reply', 'reply from client');
  });*/
});